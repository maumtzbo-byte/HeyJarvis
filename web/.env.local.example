# URL pública del backend de HeyJarvis (FastAPI).
# En desarrollo local, con el backend corriendo en tu máquina:
NEXT_PUBLIC_API_URL=http://localhost:8000

# En producción, apuntar a la URL del backend ya desplegado (Render/Railway/Fly.io).
